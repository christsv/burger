var express = require("express");

var router = express.Router();

var burger = require("../models/burger.js");

router.get(
    // to show the burgers
);

router.post(
    // to add new burgers
);

router.put(
    // when burgers are eaten
);

module.exports = router;
